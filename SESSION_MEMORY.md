# SESSION MEMORY

## 固定约束与偏好
- 称呼用户为大哥。
- 终端读写文件使用 UTF-8。
- 修改代码时优先低耦合，避免隐式共享状态和跨层直接调用。
- 变更时优先防止连环报错，先评估影响面，再做最小可用验证。

## 上一步完成了什么
- 已持续按 `index.html` 模板调整移动端首页与底部导航，修正消息页入口、底栏玻璃感、首页留白、统计数值绑定等问题。
- 已修复移动端登录页验证码与短信码布局挤出的问题，并按验证码真实比例调整展示框。
- 已将移动端入口依赖的公网资源本地化：
  - 下载 `lucide.min.js` 到 `frontend/mobile-vue/public/assets/vendor/lucide.min.js`
  - 下载 `Inter` 字体及样式到 `frontend/mobile-vue/public/assets/fonts/inter/`
  - 将 `frontend/mobile-vue/index.html` 中的 `lucide` 和 `Inter` 外链替换为本地 `assets` 路径

## 下一步打算做什么
- 如大哥继续要求，还需把剩余页面视觉细节继续向模板收敛。
- 如大哥要求彻底清理公网依赖，再继续处理其他非入口层的外链资源或模板文件。

## 关键约束 / 风险 / 阻塞
- 当前已去掉移动端入口的 `lucide` 和 `Inter` 公网依赖，但 `package-lock.json`、文档示例、以及业务 API 调用中的联网地址不属于静态资源本地化范围。
- 还未继续处理非移动端入口层的外链引用；若大哥要求“全部前端静态资源都本地化”，需要再扩大扫描与替换范围。
