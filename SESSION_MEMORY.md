# SESSION MEMORY

## 上一步实际完成了什么
- 已核对 `README.md` 与 `提交材料三_项目详细方案_正式版.md` 中与“全景分析 / 类型画像 / 相似度”相关的原文，确认这些逻辑并非空缺，而是被写在后端闭环与动态分级里。
- `README.md` 里已经明确写出：
  - `search_similar_cases` 的真实检索链路：`query -> embedding -> Redis 快照读取(缺失时回源 DB) -> 向量相似度排序 -> topK 返回`
  - 案件知识图谱画像（V1）输出：`profiles`、`graph.nodes / graph.edges`、`target_group_top_scam_types`
  - 类型画像的相似分数由三部分加权：向量中心余弦相似度 `0.6`、关键词重合度 `0.25`、目标人群重合度 `0.15`
- `提交材料三_项目详细方案_正式版.md` 里也已经写清：
  - 主智能体工具闭环包含 `query_user_info`、`search_similar_cases`、`search_user_history`、`resolve_dynamic_risk_level`
  - 动态阈值逻辑综合当前案件风险因子、历史相似案例、用户历史风险状态与 `historical_score`
  - 最终风险判级不是单纯标签，而是“当前案件评估 + 双重知识支撑 + 动态调整”
- 结论是：桌面端“全景分析 / 类型画像”更像前端展示层的命名，底层真实逻辑已经在 `README` 和提交材料三里，但不是单独以同名章节展开。
- 已按这个结论补充提交材料三：
  - 新增“管理端全景分析与类型画像的对应关系”小节
  - 明确 `全景分析 / 类型画像 / 相似度` 对应的是后端知识聚合、向量召回、动态阈值和治理闭环
  - 同步把总结部分的表述收束为“展示层承载后端能力”，避免口径漂移
- 已继续补充提交材料三中的“数据库去重与重复写入控制”小节：
  - 候选案件入库前先做字段规范化、文本清洗和向量化
  - 先与正式历史案件库做 top1 相似度比对
  - 相似度达到重复阈值时拒绝写入待审核表
  - 管理员审核通过后才正式入库，并清理待审核记录
- 已将项目演示视频脚本整体改成“讲解式演示”风格：
  - 开场、权限、Android、服务端、管理员端、Web、收尾全部收敛为讲课式台词
  - 字幕大幅压缩，只保留短标签
  - 取消剧情感表达，避免带入场景叙事
  - Android 端主线固定为 `输入 -> 处理中 -> 报告返回`
  - 管理员端主线固定为 `全景分析 -> 图谱/画像 -> 审核/去重 -> 案件库/统计`
- 已删除视频脚本末尾的“最终建议”段，避免和当前讲解式口径冲突。
- 已进一步修正画面 5 / 6 的预警表达：
  - 深度分析预警分为前台与后台两端展示
  - 悬浮窗是给用户“马上查一下”用的快速入口
  - 后台检测是系统先做一次初筛、再提醒的另一种入口
  - 两条路彼此独立，不应写成同一条快速链路
  - 完整旁白也同步改成同一口径
- 已确认最终口径：悬浮窗与后台检测都属于主动防护体系，但各自是完全独立的路径；
  - 悬浮窗：用户点一下，直接看结果
  - 后台检测：系统先筛一遍，再决定要不要提醒
- 已将“后台检测”对应旁白改成更正式的书面表达，避免口语化的“多看一眼”“各走各的”。
- 已将项目演示视频脚本的开场、权限、Android、服务端、管理员端、Web 和收尾旁白统一改成书面化表述，去掉“这是 / 先看 / 点一下 / 马上”等面对面口语开头。
- 已恢复少量“不是……而是……”对比句，但仅用于关键能力点，目的在于突出系统差异化，不回到口语化叙述。
- 已将“不是……而是……”对比句总量控制到最多 5 句，其余恢复为普通书面表述，避免整篇旁白变成反复强调式话术。
- 已把视频脚本的每个章节标题补上时间预算，方便录制时直接对齐：
  - 开场 10-15 秒
  - 痛点与需求 8-12 秒
  - 权限准备 5-8 秒
  - Android 分析闭环 40-55 秒
  - 服务端说明 20-30 秒
  - 管理员端治理 30-40 秒
  - 移动端 Web 5-8 秒
  - 测试结果 5-8 秒
  - 收尾 10-15 秒
  - 团队分工 5-8 秒
  - 未来展望 8-12 秒
- 已在视频脚本中新增“测试结果”画面，位置放在收尾前，用于展示准确率、F1-score、平均响应时延和稳定性等核心指标。
- 已在视频脚本中新增“痛点与需求”开场段，以及结尾的“团队分工”“未来展望”两段，顺序调整为“痛点前置、总结后补团队和展望”。
- 已在“详细功能”前新增“功能总览”段，并将其细化为可直接落图的模块清单：Android 端（分析提交、历史记录、趋势查看、家庭守护、演练训练、AI 助手、悬浮窗、自动守护）、后台（多模态识别、风险判定、实时告警、历史归档、家庭联动）、管理员端（全景分析、关系图谱、类型画像、案件审核、案件库、地区统计、用户管理、管理助手）。
- 已将“推荐时长”部分改成表格化排版，补充总时长、分段预算和节奏说明，便于直接作为正式提交材料内容使用。
- 按当前章节预算汇总，视频总时长约为 `2 分 38 秒 - 3 分 47 秒`，如果保留一点镜头停顿和转场缓冲，实际录制建议控制在约 `2 分 55 秒 - 3 分 35 秒`。
- 如果章节之间会插入切换画面或标题卡，整体做到 `3 分钟到 3 分 30 秒` 是合适的，且更利于保持节奏完整。
- 已核对并复述了桌面管理员端的可见入口和页面内容：
  - `全景分析`
  - `地区案件统计`
  - `用户管理`
  - `案件审核`
  - `案件库`
  - `管理助手`
  - `个人设置`
- 已确认桌面端里还有一个共享的 `家庭中心` 模板区块，但当前路由配置并没有把 `family` 作为公开 tab 暴露出来，不能当作正式演示主页面来讲。
- 已核对 Android 端的页面与底部导航：
  - 概览 / Dashboard
  - 历史档案 / History
  - 风险趋势 / RiskTrend
  - 消息 / Alerts
  - 分析 / Submit
  - 演练 / SimulationQuiz
  - AI 助手 / Chat
  - 家庭 / Family
  - 我的 / Profile
  - 隐私资料 / ProfilePrivacy
- 已确认 Android 端的关键能力还包括：
  - 任务详情抽屉 / `TaskDetailSheet`
  - 风险预警弹窗 / `AlertSheet`
  - 家庭联防弹窗 / `FamilyAlertSheet`
  - 悬浮球快捷分析
  - 无障碍自动守护
  - 通知权限与定位权限引导
- 已根据上述页面清单，重写正式提交材料里的演示视频脚本：
  - 去掉完整剧情式演绎
  - 保留极短触发场景
  - 强化 Android 分析闭环
  - 强化桌面治理展示
  - 移动端 Web 只做补充说明
- 已收到新反馈：当前字幕过短，叙事感不够，需要改成“会讲故事”的表达，而不是单纯功能标签。
- 已将视频脚本里的字幕整体改成更有叙事推进感的句子，避免只剩下功能词堆叠。

## 下一步打算做什么
- 当前阶段的视频脚本已按“讲解式演示”完成，下一步如果继续改动，只做局部措辞压缩或口播润色，不再改回剧情式表达。
- 同时保留发布会式演示，视频主线不再调整。

## 关键约束 / 风险
- 桌面管理员端不适合拍成长操作教程，重点应放在治理与统计的结果页。
- Android 端适合展示“输入 -> 处理中 -> 报告返回”的闭环，不适合长时间逐步教学。
- `family` 在桌面端模板里存在，但不是当前路由可见入口，视频中不要误说成桌面端主功能页。
