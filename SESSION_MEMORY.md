# SESSION MEMORY

## 固定约束与偏好
- 称呼全称：yyz先生。
- 终端读写文件使用 UTF-8。
- 优先低耦合处理，避免隐式共享状态和跨层直接调用。
- 避免连环报错，实施前评估影响面，错误就地处理。

## 上一步完成了什么
- 升级了移动端 Vue 3 的 UI 为“高端原生软件风格”（大圆角、模糊背景、柔和阴影、层级排版、基于风险的动态光效）。
- 完成了底层导航栏 `MobileBottomNav.vue` 的重构，增加了底部安全区适配和悬浮居中的动态光效按钮。
- 完成了个人中心/设置页 `MobileProfileScreens.vue` 的重构。
- 修复了此前发现的 `risk_level` 字段 JSON 解析不一致问题、400轮询报错。
- 解决了聊天界面消息超出屏幕宽度的问题。
- 全面修复了所有主页面（家庭、我的、历史记录、风险趋势、智能检测）的双重滚动条问题。
- 删除了家庭守护 (`family`) 页面左上角错误的返回按钮，并将标题居中对齐。
- **最新修改：删除了家庭守护 (`family`) 页面右上角的“+”号按钮（邀请按钮），并将该页面的顶部导航栏（Header）背景修改为了纯白不透明（移除了 backdrop-blur 毛玻璃和透明度效果）。**

## 下一步打算做什么
- 等待 yyz先生确认家庭守护界面的布局是否已经符合预期。
- 继续根据 yyz先生的反馈进行界面的进一步调优或者进行下一个业务模块的开发。
