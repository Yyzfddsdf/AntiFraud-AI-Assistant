# SESSION MEMORY

## 固定约束与偏好
- 称呼全称：yyz先生。
- 终端读写文件使用 UTF-8。
- 修改代码时优先低耦合，避免隐式共享状态和跨层直接调用。
- 变更时优先防止连环报错，先评估影响面，再做最小可用验证。

## 上一步完成了什么
- 已完成 geo 地图后端同源代理方案落地，前端不再直接请求第三方地图边界域名。
- 已更新 `README.md` 和 `API.md`，去掉固定外网地址，统一使用本地默认地址与 `http://<HOST>` / `ws://<HOST>` 占位符。
- 已定位“图谱按键点了没反应”的原因：
  - 图谱按钮位于管理员分析模板 `frontend/desktop-vue/DashboardAnalyticsAdminViews.template.html`
  - 图谱弹窗却错误放在任务中心模板 `frontend/desktop-vue/src/components/dashboard/views/DashboardCoreViews.template.html`
  - 导致管理员页点击按钮时，状态虽然变化，但对应弹窗节点不在当前渲染分支内
- 已修复该问题：
  - 从任务中心模板移除了图谱弹窗
  - 将图谱弹窗移动到管理员分析模板内，与 `openGraphModal` 按钮处于同一渲染链路

## 下一步打算做什么
- 由 yyz先生 在前端页面实际点击“查看全景关系图谱”按钮验证交互是否恢复。
- 如果仍有异常，再继续检查图谱数据请求、`vis-network` 渲染或首帧尺寸计算问题。

## 关键约束 / 风险 / 阻塞
- 本次修复集中在前端模板渲染层，不改后端图谱接口逻辑，耦合面较小。
- 还未做浏览器侧人工验证；若按钮能打开弹窗但画布为空，下一步应检查图谱数据是否返回以及弹窗打开后的首帧渲染时机。
