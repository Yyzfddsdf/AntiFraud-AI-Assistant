# Session Memory

日期：2026-03-26
项目：D:\Work\AntiFraud-AI-Assistant
用户称呼要求：必须使用全称 yyz先生

## 固定约束与偏好
- 品牌名不要乱改。
- 移动端左上角主标题：反诈卫士
- 移动端左上角副标题：Sentinel AI
- 桌面端左上角标题：Sentinel AI
- 优先精确定位再改，不要靠猜测反复调参数。
- 对超长模板，优先拆分，不要继续在一个大文件里缝补。
- 用户很在意是否改错平台（桌面端/移动端）。
- 用户要求桌面端反诈模拟采用“概览页缩减 + 独立答题页”的结构。
- 用户明确要求答题选项不能显示任何风险提示。
- 用户要求 `reset` 相关操作不能把未完成题目入口清空。

## 上一步完成了什么
- 桌面端反诈模拟已拆成概览页与独立答题页，概览页缩减为生成题包、`current_pack`、报告历史。
- 删除了概览页中的题目列表，只保留 `current_pack` 作为进入答题的入口。
- 修复了 `resetSimulation`：现在会先清显示态，再检测未完成会话并回填到 `current_pack`，不再把未完成题目入口抹掉。
- 取消了基于 `simulationStatus` 的强制跳转；未完成题目恢复后只刷回概览，不会自动切到答题页。
- 已通过 `npm.cmd run build` 验证桌面端前端构建成功。

## 下一步打算做什么
1. 等待 yyz先生确认当前桌面端反诈模拟的主页面缩减和 `current_pack` 行为是否符合预期。
2. 如果 yyz先生继续调整桌面端反诈模拟，优先只改概览页信息密度与按钮行为，不回退到题目列表模式。
