# 基于多模态的反诈智能助手项目文档（获奖导向版）

## 一、项目介绍

### 1.1 项目概述

本项目聚焦电信网络诈骗持续高发、诈骗手段日益智能化与多模态化的现实背景，面向普通公众设计并实现了一套“可识别、可预警、可记忆、可协同、可持续进化”的多模态反诈智能系统。系统以人工智能技术为核心，以大众真实防诈场景为落点，围绕文本、图片、音频、视频等多源可疑信息，构建了从风险识别、智能分析、结果反馈、历史沉淀到家庭联防的完整闭环。

与传统基于关键词匹配或单次人工问答的反诈工具不同，本项目不是一个仅能返回“高风险/低风险”的简单识别器，而是一个面向大众长期使用的智能反诈系统。系统既能够在用户主动提交可疑内容时给出多模态分析结果，也能够在高风险事件发生后主动推送提醒，并结合用户过往风险记录、全局历史案例知识库和家庭守护关系，形成持续性的安全防护能力。

项目在系统架构上采用后端服务与 Android 移动端协同设计。后端以 Go 为核心语言，构建多智能体分析主链路、历史案例知识库、用户个性化记忆、实时告警和家庭守护能力；移动端则面向普通用户真实使用场景，提供主动分析、风险趋势查看、聊天式反诈咨询、悬浮球快捷识别、后台提醒和无障碍自动守护等入口，使系统具备明显的产品化形态与现实落地价值。

### 1.2 项目背景

随着移动互联网、数字支付和社交平台的普及，诈骗已经由传统的短信、电话和网页链接，演变为跨平台、跨模态、跨场景的复杂风险形态。当前诈骗行为不仅利用文字话术进行诱导，还大量借助伪造截图、假冒公文、仿冒客服界面、语音诱导、短视频包装，甚至借助 AI 合成音视频制造“高度逼真”的欺骗场景。诈骗信息的表达形式更加复杂，传播路径更加隐蔽，受害对象更加广泛。

在此背景下，传统反诈模式逐渐暴露出明显局限。第一，许多现有方案仍然依赖人工审核与规则匹配，对新型诈骗和复杂语境的适应性较弱；第二，多数工具停留在用户主动查询层面，难以做到真正意义上的持续守护；第三，大众用户缺乏系统性的反诈知识积累，面对复杂诈骗情境往往难以及时形成准确判断；第四，当前大量反诈产品仍以“单用户、单次判断”为主，缺乏长期风险记忆与家庭联动能力。

因此，面向大众构建新一代反诈系统，必须从“单点识别”升级为“系统防护”：既能识别复杂多模态诈骗内容，又能理解用户个体风险背景；既能提供即时判断，又能持续沉淀经验；既能服务个人，也能实现家庭协同预警。本项目正是在这一背景下展开设计与实现。

### 1.3 项目目标

#### 1.3.1 任务要求

围绕赛题“智能计算”与“应用类”特征，本项目希望完成以下核心目标：

1. 构建支持文本、图片、音频、视频等多模态输入的诈骗风险识别系统。
2. 将人工智能能力真正落地为面向大众可使用的产品，而非仅停留在算法验证层面。
3. 实现从风险识别、报告输出、历史沉淀到主动预警的完整业务闭环。
4. 构建个性化记忆与家庭联防能力，提升系统防护深度。
5. 形成兼具展示价值、应用价值和持续演进能力的反诈智能系统方案。

#### 1.3.2 用户期望

从普通公众视角出发，用户真正需要的反诈系统应当满足以下期望：

1. 当接触到可疑内容时，能够快速获得可靠判断，而不是自己反复搜索。
2. 系统不仅识别文字，还能理解图片、语音、视频等复杂风险信息。
3. 当风险较高时，系统可以主动提醒，而不是完全依赖用户自己意识到危险。
4. 系统能够记住用户过去的风险经历，避免“同一类骗局重复发生”。
5. 家庭成员之间可以形成联动，在关键时刻帮助高风险用户及时止损。

#### 1.3.3 最终目标

本项目的最终目标不是开发一个单次使用的反诈小工具，而是构建一个可长期陪伴用户的反诈智能系统。系统能够在不同时间、不同模态、不同风险场景中持续积累知识和经验，帮助用户形成更稳定的识别能力和更及时的风险响应能力，从而实现“大众防诈从被动应对走向主动守护”的升级。

### 1.4 项目意义

本项目的意义主要体现在以下三个层面。

第一，社会意义。电信网络诈骗已成为影响人民群众财产安全和社会稳定的重要问题。面向普通公众提供低门槛、高实用的智能反诈系统，能够有效提升个人与家庭的风险识别能力，降低因信息不对称、认知不足和反应滞后带来的受骗概率。

第二，技术意义。本项目验证了多模态人工智能、向量检索、个性化记忆和实时预警机制在公共安全应用场景中的融合可行性。与单模型、单模态、单轮问答方案相比，项目更强调复杂场景理解能力和系统级闭环能力，体现了智能计算在实际应用中的综合价值。

第三，应用意义。项目已经具备较完整的产品形态和工程基础，未来可进一步扩展到社区反诈宣传、校园安全教育、家庭守护、基层治理辅助、公益反诈平台等多个方向，具有较强的推广潜力。

### 1.5 项目亮点

> 图位占位建议：`图 1 项目总体架构总览图`
>
> 图注建议：展示 Android 端、后端服务、多智能体分析、知识库、用户历史记忆、实时预警与家庭联防之间的整体关系，用于帮助评委快速建立“完整系统”认知。

> 图位占位建议：`图 2 项目核心创新机制总览图`
>
> 图注建议：展示“多智能体协同分析 + 双重防护网 + 主动守护 + 家庭联防 + 知识沉淀”的核心机制关系，用于在亮点章节直接建立项目辨识度。

#### 1.5.1 明确痛点，定位清晰

本项目不做泛化型“AI 助手”，而是明确定位为“面向大众的反诈系统”。核心解决四个问题：复杂诈骗内容难识别、风险发现不及时、用户历史经验无法复用、家庭成员难以协同干预。这样的定位使后续多模态分析、个性化记忆、主动守护和家庭联防形成统一主线，而不是零散功能堆叠。

如图 1 所示，本项目从移动端场景入口、后端分析链路、知识沉淀机制到家庭联动能力构成了完整系统，而不是单点能力组合。
如图 2 所示，项目最核心的竞争力并不只是功能数量，而是多智能体分析、双重记忆、主动守护和知识进化之间形成了统一机制。


#### 1.5.2 多模态智能分析闭环

项目支持文本、图像、音频、视频四类输入，采用“子智能体提取证据、主智能体统一决策”的闭环设计。图像负责识别截图和页面风险，音频负责识别诈骗话术，视频负责识别动态诱导内容，主智能体再结合知识库、用户历史和风险评估工具生成最终报告并完成归档。相比单模态系统，本项目更贴近真实诈骗场景，也更适合做系统级风控判断。


#### 1.5.3 通用知识库、个性化记忆与动态阈值联动

项目同时建设全局历史案件知识库和用户个人历史记忆。前者用于召回相似诈骗案例，实现“以案防案”；后者用于判断用户是否反复遇到相似骗局，实现“因人施策”。在此基础上，系统进一步引入“动态阈值判级”机制：不是让所有用户共用一条固定风险线，而是结合用户历史风险分、近期风险活跃程度以及相似案件命中结果，动态调整预警阈值。这样，系统既有公共知识能力，也有个体连续防护能力，还具备“同案不同人、不同阶段不同阈值”的个性化判级能力，是项目区别于普通问答型反诈工具的重要亮点。


#### 1.5.4 主动守护与家庭联防

项目不仅支持用户主动提交分析，还支持实时 WebSocket 告警、Android 后台提醒、悬浮球快捷识别、无障碍自动守护和家庭高风险通知。前者解决“用户来不及主动求助”的问题，后者解决“高风险用户缺少家人协助”的问题。这样，系统就从一次性识别工具升级为面向个人和家庭的持续守护系统。


#### 1.5.5 知识沉淀与持续进化

系统通过“典型案例送审 - 管理审核 - 正式入库 - 后续召回”的机制实现知识沉淀与持续进化。我们没有采用“互联网案例抓取后直接全自动入库”的方式，而是有意识地设计为“自动采集与清洗 + 人工审核把关”的半自动流程。原因在于，反诈知识库直接参与风险判断，若完全自动入库，容易把噪声样本、错误案例甚至被篡改的信息直接带入后续识别链路，反而放大误判风险。配合后台案件采集、知识图谱画像、统计总览和风险趋势分析，平台既能不断吸收新案例，也能在安全可控的前提下持续扩充知识。因此，项目不是静态识别系统，而是具备持续学习能力和治理能力的反诈平台。

#### 1.5.6 聊天式辅助决策补充能力

除固定分析主链路外，系统还提供聊天式辅助决策能力，能够结合用户历史、知识库和当前上下文进行追问、解释与建议输出，使项目既能做结构化风险判断，也能进行更灵活的连续交互。


## 二、需求分析

### 2.1 需求调研

当前大众反诈场景具有三个显著特征。第一，诈骗内容载体高度多样，用户接收到的往往不是结构化文本，而是聊天截图、语音片段、视频画面、转账提示页或跨平台传播的复杂信息。第二，普通用户在识别风险时缺乏专业知识和清晰的判断依据，面对新型骗局时容易依赖直觉。第三，用户对风险的感知通常存在滞后性，真正意识到被骗时往往已经接近发生转账、授权、验证码泄露等关键行为。

结合当前反诈产品现状，可以发现大量已有方案仍然存在明显局限：一是过度依赖用户主动提问，缺乏持续守护能力；二是只处理单一模态输入，无法适应真实诈骗场景的复杂性；三是缺少用户历史记忆与家庭协同功能，导致系统只能“看当前”，不能“看长期”；四是知识沉淀不足，难以快速吸收新型诈骗模式。

因此，本项目的需求不是单纯做一个更聪明的问答系统，而是构建一套能够服务大众真实生活场景的反诈智能系统。

### 2.2 功能需求

围绕赛题要求与大众防诈场景，系统需满足以下功能需求：

1. 支持多模态风险输入，涵盖文本、图片、音频、视频。
2. 支持多模态联合分析，并输出结构化风险等级、理由和综合报告。
3. 支持单图快速风险识别，满足手机端即时判断需求。
4. 支持聊天式反诈咨询，帮助用户获得持续交互式指导。
5. 支持用户历史案件归档与风险趋势展示。
6. 支持相似案例知识检索和个性化历史记忆召回。
7. 支持中高风险主动预警与移动端后台提醒。
8. 支持家庭关系建立、守护关系配置和家庭高风险联动通知。
9. 支持典型案例审核入库、后台案件采集和管理员侧治理分析。

### 2.3 业务分析

#### 2.3.1 用户特点及其面临的问题

本项目面向的核心用户为普通公众，包括学生、青年职场人、中老年用户以及网络安全知识相对薄弱的手机用户。该群体在面临诈骗时往往有以下共同问题：

1. 面对复杂诈骗内容时难以快速判断真伪。
2. 在高压诱导场景下容易被“退款”“安全账户”“客服回访”“验证码确认”等话术影响。
3. 遇到图像、语音、视频等复杂内容时难以综合识别。
4. 历史上即使接触过类似骗局，也很难形成系统化记忆。
5. 在高风险情况下往往缺少家庭成员的及时干预与协助。

#### 2.3.2 业务流程

系统围绕大众反诈场景形成如下业务流程：

1. 用户通过 Android 端提交可疑文本、图片、音频或视频。
2. 后端创建任务并触发多模态子智能体并发分析。
3. 主智能体聚合文本与多模态洞察，并调用用户信息查询、相似案例检索、用户历史召回和风险评估工具。
4. 系统输出高、中、低三档风险等级及结构化综合报告。
5. 分析结果写入历史归档，并同步生成用户历史语义记忆。
6. 当结果为中高风险时，通过实时通道向用户主动推送提醒。
7. 当结果为高风险且命中守护关系时，触发家庭联防通知。
8. 当案件具有典型性时，进入待审核案例队列，经管理员审核后沉淀为知识库。

#### 2.3.3 用户期望与系统回应关系

为了真正贴近大众使用场景，项目将“用户期望”转化为系统能力：

1. 用户希望快速得到判断，系统提供单图快速分析与多模态任务分析双链路。
2. 用户希望系统能理解复杂证据，系统支持文本、图像、音频、视频多模态输入。
3. 用户希望系统能持续记住风险，系统提供历史归档与个性化记忆召回。
4. 用户希望关键时刻有人提醒，系统提供主动预警与家庭联防机制。
5. 用户希望不是只看一次结果，系统提供聊天咨询、趋势分析和知识问答能力。

### 2.4 市场与应用环境分析

#### 2.4.1 社会环境

在全民移动互联网背景下，诈骗已深度渗透至大众日常生活。社交聊天、短视频平台、电商客服、兼职招聘、理财投资、教育培训等高频场景均可能成为诈骗入口。社会迫切需要能够真正服务普通公众、并具备主动守护能力的智能反诈工具。

从社会治理角度看，诈骗问题已经不再只是个体财产损失问题，而是影响社会信任、家庭安全感和数字生活秩序的重要公共安全问题。普通用户面对复杂诈骗内容时，往往处于信息弱势地位，尤其是老年人、学生和网络经验不足群体，更容易在高压诱导下做出错误决策。因此，一个真正可被大众使用、可在关键时刻进行风险干预的反诈系统，具有明显的社会现实意义。

#### 2.4.2 政策环境

近年来，国家对电信网络诈骗治理的重视程度持续提升，反诈工作已成为数字社会治理的重要组成部分。围绕网络安全、数据安全、数字政府建设、基层治理和智慧社会建设，多地都在推动人工智能、大数据和智能感知技术在公共安全场景中的应用。反诈不再只是单一宣传教育问题，而被视为需要技术赋能、平台支撑和协同治理的综合性问题。

在这一背景下，本项目以人工智能为核心、以大众反诈为场景，符合当前“科技赋能社会治理”和“智能计算落地真实应用”的发展方向。项目强调的是面向公众可感知、可使用、可落地的防护能力，而非停留在实验室算法验证层面，因此具备较强的政策契合度和公共价值表达空间。

#### 2.4.3 技术环境

近年来，多模态大模型、Embedding 检索、WebSocket 实时通信、移动端无障碍能力、MediaProjection 截屏分析等技术快速发展，为构建新型反诈系统提供了坚实基础。本项目将这些技术有机融合，使“识别、记忆、预警、联动”成为可能。

从技术成熟度看，当前多模态模型已经能够较好地处理图像、文本、音频等异构信息，向量检索技术也为相似案例召回和个性化记忆提供了可靠路径；移动端前台服务、通知系统、悬浮窗和无障碍能力的发展，则使“主动守护”从概念走向工程可实现。正因为这些技术条件已经具备，反诈系统才有机会从传统关键词规则工具升级为真正的多模态智能系统。

#### 2.4.4 经济环境

电信网络诈骗带来的损失不仅体现在受害者个体层面，也会进一步影响家庭财务稳定、社会治理成本和基层服务压力。传统人工反诈方式虽然必要，但在面对海量、多变、跨平台的诈骗信息时，存在人力成本高、响应效率有限、覆盖范围不足等现实问题。因此，利用人工智能技术提升识别效率和预警及时性，具备明显的成本效益优势。

对普通用户而言，一个低门槛、可在手机上直接使用的反诈系统，能够显著降低因认知不足导致的损失风险；对社区、学校、基层治理单位而言，这类系统也有望降低宣教和人工筛查成本，提高风险识别和劝阻效率。从这个意义上看，本项目不只是技术尝试，也具备明确的现实经济价值。

#### 2.4.5 应用环境

本项目适用于个人反诈、家庭联防、社区安全宣传、校园反诈教育、基层治理辅助和公益反诈平台等多种场景。项目兼具展示价值与推广潜力，具备从比赛作品走向更广泛应用场景的基础。

如果从场景落地角度进一步看，本项目具备较强的真实应用延展性：

1. 在个人用户场景中，系统可以作为日常反诈助手，帮助用户快速分析可疑内容并管理自身风险历史。
2. 在家庭场景中，系统可以通过守护关系与高风险通知机制，为老年人、未成年人和重点保护成员提供协同防护。
3. 在学校和社区场景中，系统可以作为宣传与实践结合的反诈辅助平台，让用户通过真实案例和交互式分析提升识别能力。
4. 在基层治理和公益平台场景中，系统可以作为技术支撑工具，帮助工作人员快速理解案件、沉淀案例并开展针对性宣教。

因此，本项目的真实意义不仅在于“做出一个能跑的系统”，更在于它为面向大众的反诈产品化、平台化和协同治理提供了一种具有现实可行性的技术路径。

## 三、核心技术与智能分析方案

### 3.1 模型总体设计

> 图位占位建议：`图 3 多智能体总体分析流程图`
>
> 图注建议：展示文本/图像/音频/视频输入进入子智能体，再由主智能体调用工具链生成报告、归档历史并触发联动的总体流程。

本项目采用“多智能体协同 + 知识增强 + 任务闭环”的总体技术设计。与传统由单一模型直接给出自然语言回复的方式不同，本项目针对不同模态设置了专门的子智能体，用于分别抽取图像、视频、音频等模态中的结构化风险线索；随后由主智能体汇总文本和多模态洞察，并结合知识库、个体历史记忆和工具链完成风险判断。

如图 3 所示，系统的核心并不是单模型直接输出结论，而是多模态子智能体先完成证据提取，再由主智能体统一进入工具闭环。

这种架构的优势在于：

1. 模态级任务与最终决策任务解耦，避免单一模型同时承担所有职责。
2. 每种模态均有清晰的证据提取过程，增强结果可解释性。
3. 主智能体通过工具闭环与持久化机制控制输出质量和终态一致性。

从处理流程上看，系统将一次完整分析拆解为“输入标准化 - 子模态分析 - 主智能体聚合 - 工具闭环调用 - 结构化报告生成 - 历史归档与事件联动”六个阶段。其中，子智能体只负责证据提取，不直接落库、不直接写最终报告；主智能体承担综合判断责任，并通过显式工具调用保证关键步骤都被记录和控制。这种设计避免了传统单模型方案中“结果依赖一次性生成、难以复核”的问题，更适合高风险、强解释需求的反诈场景。

### 3.2 多模态联合分析机制

项目围绕真实诈骗场景中的信息复杂性，构建了多模态联合分析引擎。系统支持以下四类输入：

1. 文本：案情描述、聊天记录、通话内容转写等。
2. 图像：聊天截图、转账截图、伪造通知、公文图片、页面截图等。
3. 音频：语音通话录音、语音消息、可疑诱导音频等。
4. 视频：短视频、录屏内容、动态诈骗宣传片段等。

在分析过程中，系统先对输入进行规范化，再交由对应子智能体提取风险线索。图像侧识别诱导文本、伪装界面和转账特征；音频侧识别话术诱导、冒充语境和风险表达；视频侧识别动态场景中的诈骗线索；文本侧则作为主场景语义输入参与最终聚合。主智能体在此基础上综合判断，形成结构化报告。

具体而言，图像分析尤其适用于识别聊天截图、转账页面、假冒客服通知、伪造公文和验证码页面等高频风险载体；音频分析适用于识别冒充客服、退款诈骗、投资引导、诱导转账等典型话术；视频分析则可用于处理含字幕、场景切换和动态诱导信息的可疑内容。四类输入共同服务于同一条案件分析链路，使系统能够更接近真实受骗场景中的信息复杂度，而不是人为将风险问题简化为单一文本分类问题。

### 3.3 主智能体工具闭环

主智能体并非简单生成一段结论，而是通过一组显式工具完成闭环式决策。当前系统工具链包括：

1. 用户信息查询工具。
2. 全局相似案例检索工具。
3. 用户历史案件检索工具。
4. 用户近期标签更新工具。
5. 当前风险评估工具。
6. 动态风险等级解析工具。
7. 最终结构化报告提交工具。
8. 用户历史归档写入工具。
9. 典型案例送审工具。

其核心逻辑是“先取证，再检索，再评估，再归档”。这种方式保证了模型输出不只是主观判断，而是建立在可追踪工具调用与可落库状态更新之上，从而提升了系统稳定性、可解释性与工程完整性。

在工程实现上，主智能体只有在“最终结构化报告已提交”和“用户历史案件已归档”两个条件同时满足后，任务才会被视为真正完成。这一点非常关键，因为它意味着系统不是在生成一段回答后就结束，而是必须完成状态落库、历史记录持久化和后续联动准备，才能形成完整终态。对于高风险案件，这种闭环机制还能进一步为实时预警、家庭通知和案例送审提供统一触发基础。

### 3.3.1 Prompt 设计说明

为了避免系统沦为“一个会说话的大模型”，本项目专门围绕反诈场景设计了成体系的 Prompt 机制。Prompt 的作用并不只是优化语言风格，而是直接参与多智能体分工、工具调用顺序、风险判断边界和输出结构控制。项目当前已形成“主智能体 Prompt + 图像/视频/音频子智能体 Prompt + 单图快速识别 Prompt + 聊天助手 Prompt + 案件采集 Prompt”的多层提示词体系，对应不同任务分别约束。

在角色设计上，主智能体被限定为“总控与决策者”，负责综合分析、工具编排和终态闭环；图像、视频、音频子智能体则只负责模态级取证，不直接输出最终风险等级、不直接写库。这种“取证与裁决分离”的 Prompt 设计，使不同模型不会在同一轮中同时承担提取证据、推断风险和写入状态等多重职责，有助于降低推理漂移并提升可解释性。

在过程控制上，主智能体 Prompt 明确把完整任务拆成“信息收集与补全 - 当前案件评分 - 动态风险分级 - 最终报告提交 - 典型案例沉淀 - 历史归档结束”六个阶段，并对工具调用顺序做了显式约束。也就是说，模型不能跳过 `submit_current_risk_assessment` 自行编造分数，不能绕过 `resolve_dynamic_risk_level` 自行改写风险等级，也不能在未归档前把任务视为完成。关键状态都必须由工具链返回并落库，从 Prompt 层面保证了系统行为稳定可控。

在反幻觉设计上，Prompt 明确要求“先证据，后结论”。系统在做最终判定前，优先检索全局知识库和用户个体历史，并要求结论围绕风险信号、相似命中、评分结果和动态阈值展开解释，而不是让模型直接给出无法复核的主观判断。对于证据不足的场景，Prompt 也要求模型给出保守判断和不确定性边界，避免强行补全。

在输出设计上，Prompt 配合工具链统一收敛到结构化字段，包括风险因子、风险等级、风险理由、下一步动作、可选的诈骗链路还原与关键词句。这种设计既便于前端展示，也便于测评集批量运行和评估报告统计。更重要的是，它让 Prompt Engineering 与赛题要求中的“工具调用、知识库检索、动态评估、报告生成”形成了直接对应关系。

本项目《Prompt设计说明》已单独整理，详见：`competition_docs/Prompt设计说明.md`。

### 3.4 通用知识库与个性化记忆

> 图位占位建议：`图 4 双重防护网结构图`
>
> 图注建议：展示“全局历史案件知识库”和“用户个性化历史记忆”两条检索路径如何共同参与主智能体判断。

本项目在智能分析中构建了“双层记忆机制”。

如图 4 所示，主智能体在判断时同时接入全局历史案件知识库和用户个性化历史记忆，两条检索路径共同支撑最终风险决策。

第一层为全局历史案件知识库。系统通过向量化语义表示对诈骗案例进行索引管理，在新案件分析时可以召回语义相似的历史诈骗案例，为模型提供“已知骗局”的参考。

第二层为用户个性化历史记忆。系统将用户已归档历史案件进行单独向量化存储，在分析当前风险时，除检索全局知识外，还检索“该用户过去是否遇到过相似骗局”。这种设计使系统具备跨时空关联能力，不再局限于单轮输入。

这也是本项目的重要创新之一，即构建了“防诈知识库 + 个性化记忆系统”的双重防护网。

从实际效果看，这一机制有效解决了传统系统“只知道常见套路，不理解用户个人风险背景”的问题。对于从未见过的用户，系统可以依赖全局知识库给出专业判断；对于已有历史记录的用户，系统则可以进一步判断其是否存在重复受骗模式、特定高频风险类型或近期风险变化趋势。这种双层召回机制使系统具备更强的连续防护能力。

### 3.5 风险评估与动态分级

系统采用“当前案件风险分 + 用户历史风险分 + 相似命中修正 + 动态阈值”机制，而不是简单依赖固定标签。风险等级判断既考虑当前输入中的风险强度，也考虑该用户近期是否处于高风险暴露状态。其核心设计思路如下。

第一，系统会对当前案件进行多维风险评分。评分不是只看某一个单点信号，而是综合社会工程学特征、危险动作请求、多模态证据强度和受害动作阶段等多个维度进行结构化评估。对于远程控制、验证码索取、转账要求、私人账户收款等高危行为，系统会做额外提升，避免明显危险场景被低估。

第二，系统为每个用户维护历史风险分。这个分数不是简单统计“历史上出现过几次风险”，而是引入了时间衰减算法、严重度增益机制和饱和映射机制，对历史案件做连续加权。也就是说，近期发生的高风险案件影响更大，久远且轻微的案件影响会逐渐减弱，从而让系统更准确地刻画用户当前所处的真实风险状态。

第三，系统根据历史风险分动态调整预警阈值。传统方案往往给所有用户设置同一条固定风险线，而本项目会根据用户的历史风险暴露情况自动调整阈值：历史风险越高、越频繁接触高危场景的用户，系统越倾向于提前预警；历史风险较低、风险状态稳定的用户，系统则保持更克制的提醒策略，以降低误报和打扰感。

第四，系统还会把全局知识库命中结果和用户历史相似案件命中结果作为修正信号引入最终判级。如果当前案件与高风险历史案例高度相似，系统会主动提高警惕；如果更接近低风险或正常模式，系统则会做相应回调。这种设计使系统既能看到“当前内容像不像诈骗”，也能看到“对这个用户来说是否更危险”。

因此，本项目最终形成了“当前风险评估 + 历史风险建模 + 动态阈值调节 + 相似命中修正”的统一判级链路。系统输出的不只是一个高、中、低标签，还能给出结构化风险原因与综合报告，使用户不仅知道“风险高不高”，还知道“为什么高、为什么这次要特别警惕”。这也意味着，本项目真正实现了个性化风险分级，而不是让所有用户共用同一条静态分界线。

除此之外，系统还在结构化报告中进一步支持诈骗链路还原、风险关键词提取、命中规则说明和核心风险摘要，便于前端将复杂分析结果转化为普通用户可理解的展示形式。例如，用户不仅能看到“当前风险为高”，还可以看到“风险来源于退款诱导、验证码索取、仿冒客服话术”等具体依据，从而增强风险判断的可理解性和劝阻效果。

#### 3.5.1 动态阈值案例对照验证

> 图位占位建议：`图 4-1 同事件双用户动态阈值对照图`
>
> 图注建议：展示“同一冒充领导付款指令事件”在两个不同历史风险用户上的判级差异。建议使用 `test/scam-report-TASK-CF99F1C1B462-2026-03-22.md`、`test/scam-report-TASK-D80225058E48-2026-03-22.md` 和对应截图做对照，突出 `historical_score`、动态阈值、当前案件分、最终风险等级之间的差异，用于证明动态阈值机制在真实案例中的有效性。

为避免“只讲原理、不见证据”，本节建议补充一组同事件双用户案例对照。该组案例的价值在于：案件文本基本一致，诈骗类型一致，用户都采取了电话核实动作，但由于两名用户的历史风险状态不同，系统给出了不同的最终风险等级。这能够直观说明本项目的动态阈值机制不是停留在概念层，而是已经能够在真实案例中体现“同案不同人、分级不同”的判级效果。

建议对照展示以下字段：

1. 当前案件基础分。
2. 用户历史风险分。
3. 动态阈值。
4. 知识库命中情况与用户历史命中情况。
5. 最终风险等级与判定理由。

### 3.6 知识沉淀与持续进化机制

为了使项目具备长期成长能力，系统设计了典型案例送审与知识入库机制。对于分析过程中识别出的典型案件，主智能体可将其提交至待审核案件库。管理员审核通过后，案例才正式进入历史案件知识库，并参与后续相似检索。

与此同时，系统还支持管理员发起后台案件采集任务，由智能体联网检索特定主题案例并逐条写入待审核区。这使系统能够持续吸收新型诈骗样本，保持知识新鲜度。需要特别强调的是，本项目并不追求“全自动抓取 - 全自动入库”的激进路线，而是刻意采用半自动进化机制：采集、初筛、字段规范化、去重等步骤尽量自动化，但正式入库之前必须经过人工审核。

这样设计的原因在于，反诈知识库不是普通内容库，而是会直接影响后续风险判断的高敏感知识资产。若完全自动入库，一旦抓取到失真案例、过时案例、媒体转述误差或不完整信息，系统就可能把错误知识继续传播到相似检索、风险判级和报告生成中。因此，我们将“安全性与知识质量”优先级放在“全自动”之上，通过人工审核维持知识库可信度。

为了控制知识质量，系统在待审核写入前还会进行基础字段规范化、向量相似度去重和输入质量校验，避免低质量样本、噪声样本和重复案例直接进入正式知识库。审核通过后的典型案例将进一步参与全局相似案例检索与知识图谱画像分析，构成“自动采集 - 自动清洗 - 人工审核 - 正式入库 - 后续再利用”的知识治理闭环。

### 3.7 移动端场景化主动防护机制

> 图位占位建议：`图 5 Android端主动识别流程图`
>
> 图注建议：展示高风险页面、悬浮球快捷触发、无障碍自动守护、截图/OCR、快速判断、进入主分析、触发预警与家庭联动的完整链路。

本项目的一项突出亮点，是将后端复杂的多模态分析能力真正下沉到了移动端高频风险场景之中。不同于多数反诈系统只能停留在“用户打开应用后手动上传内容”的被动模式，本项目进一步构建了面向真实手机使用环境的场景化主动防护机制，使风险识别可以直接发生在聊天、网页浏览、支付操作、应用下载等诈骗高发页面。

如图 5 所示，移动端主动防护链路覆盖了快捷触发、自动识别、快速初判、主分析接入以及后续预警联动，体现了项目在产品化和实用性上的差异化优势。

在具体实现上，系统提供了悬浮球快捷识别与无障碍自动守护两类入口。用户在感到页面可疑时，无需重新整理文字或切换至复杂表单页面，只需通过快捷入口即可直接触发当前屏幕的风险识别流程；对于特定高风险场景，系统还能够结合页面文本特征和截图信息进行自动识别与提醒。识别结果并非停留在简单提示层，而是可以与后端主分析链路打通，在必要时继续完成深度分析、历史归档、风险预警和家庭联动。

这一机制的真正价值，在于它把“反诈”从事后查询工具，推进为事中可介入的移动端安全能力。对评委而言，这不仅体现了项目具备后端分析能力，更体现了团队已经考虑到普通用户在真实受骗场景中的操作习惯和反应时间，将系统从“能分析”进一步推进到“能在关键时刻真正用起来”。

### 3.8 聊天式反诈辅助决策机制

除了固定的多模态分析链路外，项目还专门构建了聊天式反诈辅助决策机制，用于承接那些“信息不完整、用户表达零散、需要追问和建议”的场景。现实中，很多用户并不会一开始就提交完整证据，而是只会说“对方让我转账”“这个页面是不是有问题”“我刚刚接到一个电话不知道是不是诈骗”。如果系统只有固定分析链路，就容易出现输入不完整、交互不自然、建议不够灵活的问题。

聊天模块的价值就在于对这一缺口进行补充。系统在聊天过程中不仅可以调用全局历史案件知识库，还可以主动结合当前用户的历史案件、近期标签、风险概览等上下文信息，生成更贴近该用户实际情况的回答和建议。也就是说，聊天模块并不是一个与主分析链路割裂的“普通问答功能”，而是建立在用户历史记忆、知识检索和工具调用基础上的动态辅助决策能力。

在对话历史存储与检索方面，项目当前采用的是“按用户维度的 Redis 会话上下文”方案。系统会以 `chat:context:<user_id>` 为键保存当前用户最近一段会话中的用户消息、助手消息以及工具调用结果，从而保证下一轮对话不是孤立回答，而是建立在上一轮澄清、工具查询和建议输出基础上的连续交流。当前会话上下文默认 TTL 为 5 分钟；在用户持续对话时，系统会在读取历史后自动刷新 TTL，保持活跃会话不被过早回收；若用户需要重新开始新话题，也可以通过刷新接口主动清空上下文。

这种设计有三个现实意义。第一，它能满足赛题中“多轮对话上下文一致性”的要求，使聊天助手能够记住用户刚刚提供的身份、风险场景和追问结果。第二，它不会像长期永久存储那样无限扩大会话噪声，而是保留“最近有效上下文”，更适合反诈场景下短时、高价值的连续问答。第三，系统对外提供了上下文查询和刷新能力，既便于前端展示当前会话状态，也便于在演示和排障时快速确认上下文是否生效。

这种机制使系统形成了“两条互补路径”：一条是面向结构化输入的固定分析主链路，适合完整案件识别与正式归档；另一条是面向模糊表达和持续咨询的聊天辅助链路，适合追问、解释、提醒和建议输出。前者保证系统判断能力，后者增强系统陪伴能力与灵活性。两者结合后，项目不再只是一个“会分析一次内容”的工具，而是一个既能做正式判断，也能做连续交互引导的智能反诈系统。


## 四、系统设计与实现

### 4.1 系统总体架构

> 图位占位建议：`图 6 系统总体设计图`
>
> 图注建议：正式展示系统三层结构，包括 Android 交互层、后端业务服务层、数据库与缓存层，以及主要业务模块边界。

系统总体由三层构成：

如图 6 所示，系统在总体设计上形成了“Android 交互层 - 后端业务服务层 - 数据与缓存层”的三层结构。

1. 用户交互层：Android 移动端，提供分析入口、历史查看、聊天咨询、家庭中心和主动守护能力。
2. 业务服务层：Go 后端，负责鉴权、分析任务管理、主智能体编排、知识库管理、实时预警与家庭联动。
3. 数据与缓存层：主业务数据库、历史案件知识库、用户历史语义索引与 Redis 缓存。

整体架构采用模块化解耦设计，后端主要由登录系统、聊天系统、用户画像系统、家庭系统、多智能体分析系统、案件知识库模块、风险总览模块和缓存模块组成。各模块职责边界清晰，有利于迭代与扩展。

从系统职责划分上看，登录系统负责用户认证、权限边界与安全控制；用户画像系统负责职业、年龄、近期标签等个体信息维护；聊天系统负责对话式反诈咨询与工具调用；多智能体分析系统承担多模态任务分析主链路；案件知识库模块负责典型案例管理、相似检索和知识运营；家庭系统负责家庭关系与守护通知；风险总览模块负责历史趋势、统计聚合和总览输出；缓存模块则统一承担验证码、限流、聊天上下文与向量快照的缓存管理。通过这样的模块分工，系统既保持了工程清晰度，也便于在参赛表达中突出“完整系统”而非“若干功能集合”。

### 4.2 后端实现设计

> 图位占位建议：`图 7 后端模块架构图`
>
> 图注建议：展示登录系统、用户画像系统、聊天系统、家庭系统、多智能体分析系统、知识库模块、风险总览模块和缓存模块之间的关系。

后端系统采用 Go + Gin 实现 HTTP 服务，结合 GORM 进行数据访问和持久化管理。其核心实现包括：

如图 7 所示，后端并非单体式堆叠实现，而是围绕登录、画像、聊天、家庭、多智能体分析、知识库、风险总览和缓存等模块形成清晰边界。

1. 登录注册、验证码、短信验证码、JWT 鉴权和管理员权限控制。
2. 多模态分析任务创建、异步调度、任务状态流转和详情查询。
3. 多智能体分析主链路与工具注册体系。
4. 历史案件知识库管理、待审核案件管理和后台案件采集。
5. 用户历史案件归档、风险趋势总览与历史向量索引。
6. WebSocket 实时预警与家庭通知推送。

后端任务链路强调完整闭环。任务状态由统一状态层维护，确保从 `pending` 到 `processing` 再到 `completed/failed` 的流转清晰可追踪；历史案件归档、案例送审和家庭高风险联动都在主链路上有明确位置，从而保障系统逻辑完整性。

在接口组织上，系统将公开认证接口、普通业务接口和管理员治理接口分层挂载，避免权限混杂。普通用户可访问登录注册、聊天分析、历史记录、趋势查看、家庭管理等接口；管理员则可进一步访问知识库案例管理、待审核案例管理、后台案件采集、统计总览和图谱分析接口。这样的设计既贴合真实业务权限逻辑，也有利于项目在比赛中展现“平台治理能力”。

在状态管理上，系统采用统一状态存储层维护任务创建、处理中、完成、失败等生命周期，并通过事务保障“写历史 + 删待处理”的一致性。这使多模态异步任务不会因为中间异常而出现脏状态，有利于保障系统稳定运行。

### 4.3 Android 移动端实现设计

> 图位占位建议：`图 8 Android端模块架构图`
>
> 图注建议：展示 MainActivity、ViewModel、Repository、BackgroundAlertService、QuickAnalyzeOverlayService、AccessibilityAutoAnalyzeService 等核心模块协作关系。

移动端采用 Android + Compose 实现，围绕普通用户的高频使用需求构建产品界面。当前已形成如下功能布局：

如图 8 所示，Android 端由页面层、状态管理层、数据访问层和多个守护型服务共同组成，形成了较完整的端上能力结构。

1. 登录注册与用户中心。
2. 多模态分析提交页面。
3. 风险历史与任务详情页面。
4. 风险趋势总览与可视化展示。
5. 聊天式反诈助手。
6. 家庭中心与守护关系配置页面。
7. 后台风险预警通知。
8. 悬浮球快捷识别。
9. 无障碍自动守护。

这意味着项目不仅有后端能力，也真正具备了面向大众的产品外壳和真实可演示的终端入口。

在交互设计上，移动端并未仅仅充当请求接口的载体，而是根据大众使用习惯形成了多条不同入口。对于主动型用户，可以直接进入分析页上传文本、图片、音频和视频；对于希望获得持续咨询的用户，可以使用聊天式反诈助手；对于希望查看长期状态的用户，可以在风险历史和趋势页面中看到个人风险分布与变化；对于高风险实时场景，系统提供后台通知、悬浮球快捷识别和自动守护能力。这种多入口设计提升了项目的真实产品感，也更符合“面向大众”的主题。

特别是悬浮球快捷识别与无障碍自动守护两项能力，将后端复杂分析能力转化为移动端低门槛使用动作。用户在感到可疑时只需点击悬浮球，即可快速获取屏幕级风险判断；在开启自动守护后，系统还能在特定高风险文本场景下自动触发分析与提醒。这些能力显著增强了项目的现场演示表现力和实际使用价值。

### 4.4 数据库与缓存设计

> 图位占位建议：`图 9 数据库与缓存协同图`
>
> 图注建议：展示主业务数据库、历史案件数据库、用户历史向量索引和 Redis 缓存之间的职责划分与数据流向。

项目采用双数据库隔离方案：

如图 9 所示，主业务数据库、历史案件数据库、用户历史向量索引和 Redis 缓存分别承担不同职责，共同支撑系统的稳定性与检索效率。

1. 主业务数据库负责用户、任务、历史记录、家庭关系、通知等业务信息。
2. 历史案件数据库负责典型诈骗案例知识与向量表示存储。

此外，Redis 统一负责验证码缓存、请求限流、聊天上下文、向量记录缓存等能力。通过缓存与数据库协同，项目在保证一致性的同时提升了查询效率和响应性能。

这种双数据库设计的意义在于，将“用户业务事实”与“诈骗知识资产”进行边界隔离。主业务数据库强调稳定性和事务一致性，适合承载用户状态、任务状态和家庭关系；历史案件数据库强调知识管理和检索能力，适合承载典型案例、向量表示和治理结果。这样既便于后续知识库独立扩展，也避免知识治理逻辑过度侵入主业务流程。

在参数管理上，系统支持配置文件与环境变量结合的方式，便于在测试环境与生产环境之间进行安全隔离和快速切换。

在缓存层面，系统将验证码、限流、聊天上下文和向量快照统一收敛到 Redis，既减少了业务模块之间重复实现缓存逻辑的成本，也为多实例部署和响应性能优化提供了基础。尤其是在知识库检索场景下，向量记录快照缓存能够显著减少重复读取和重复计算开销。

### 4.5 实时预警与家庭联动实现

> 图位占位建议：`图 10 风险事件联动图`
>
> 图注建议：展示风险分析结果如何从历史归档流向 WebSocket 个人提醒，再流向家庭通知模块，形成个人与家庭双层联动。

项目在风险事件联动上形成了两级机制。

如图 10 所示，风险事件在历史归档后可继续触发个人预警与家庭通知，形成“识别结果 - 事件联动 - 多端提醒”的扩展链路。

第一级为个人风险主动提醒。系统在识别到中高风险案件后，通过 WebSocket 向移动端主动推送提醒，移动端可以在前台界面和后台通知中接收信息。

第二级为家庭联防通知。当用户被配置为被守护成员，且出现高风险案件时，系统会向对应守护人发送家庭高风险通知，实现家庭内部的协同干预。这使项目从个人自用工具升级为家庭协同防护系统。

从工程角度看，这一联动机制采用的是“风险事件回调 + 家庭系统订阅”的低耦合方案。多模态分析主链路不直接操作家庭关系表，而是在历史归档后统一产出风险事件，再由家庭系统根据守护关系判断是否生成通知。这样既能保障主分析流程聚焦核心识别逻辑，也能避免家庭模块反向侵入分析系统，实现模块边界清晰和后续可维护性。

## 五、项目创新点

### 5.1 从单次识别到系统闭环的创新

> 图位占位建议：`图 11 系统闭环创新图`
>
> 图注建议：展示“输入 - 分析 - 报告 - 归档 - 预警 - 联动 - 入库 - 再利用”的完整闭环，突出与单次识别型项目的差异。

多数同类项目的终点是“给出一次风险判断结果”，而本项目将风险识别进一步扩展为完整业务闭环：多模态输入进入分析任务后，不仅生成风险等级和结构化报告，还会继续完成历史归档、个性化记忆写入、实时预警触发、家庭通知联动和典型案例送审。也就是说，本项目的创新不在于单个模型本身，而在于把识别、反馈、沉淀和后续利用整合为一条完整可运行链路，使系统具备持续服务能力，而非一次性演示能力。

如图 11 所示，项目的创新点首先体现在系统闭环完整，而不是局限于某一个识别步骤。

### 5.2 从通用识别到个体连续防护与动态阈值判级的创新

传统反诈系统即使引入知识库，也多停留在“基于公共案例给出统一判断”的层面，很少进一步考虑用户个人历史风险背景，更少有系统真正把“用户历史风险状态”写入判级阈值。本项目构建了“全局历史案件知识库 + 用户个性化历史记忆 + 动态阈值判级”的三层机制，使系统既能参考典型诈骗案例，又能判断该用户是否反复遭遇相似骗局，并进一步把这些历史风险信息映射为动态阈值。其关键不在于某个固定公式，而在于我们把时间衰减、风险严重度增益、相似命中修正和分段阈值调节统一整合进判级流程，使系统能够根据用户风险状态变化而变化。这样的设计将反诈从“通用智能识别”推进到“个体连续防护”，让系统具备了更强的针对性、前瞻性和长期价值。

### 5.3 从被动查询到主动介入的创新

> 图位占位建议：`图 12 主动守护创新机制图`
>
> 图注建议：展示“用户主动提交”与“系统主动介入”两种模式对比，突出项目在移动端主动防护上的创新。

多数反诈工具只有在用户主动打开、主动上传、主动提问时才会发挥作用，而真实受骗场景中，用户往往没有足够时间完成复杂操作。本项目将反诈能力前移到移动端高频风险场景中，通过 WebSocket 风险预警、Android 后台提醒、悬浮球快捷识别和无障碍自动守护等方式，实现从“用户主动求助”到“系统主动介入”的转变。这种设计显著提高了系统在真实生活场景中的可用性，也是项目产品化能力的重要体现。

如图 12 所示，系统同时覆盖“用户主动触发”和“系统自动介入”两类模式，使反诈能力更贴近真实使用环境。

### 5.4 从个人工具到家庭协同机制的创新

> 图位占位建议：`图 13 家庭联防机制图`
>
> 图注建议：展示家庭组、守护关系、高风险案件和家庭通知之间的逻辑关系，突出“个人防护 + 家庭联防”的结构创新。

当前多数反诈产品默认用户是独立个体，系统最多向本人返回判断结果，但现实中很多高风险用户最需要的是家人在关键时刻介入。本项目进一步引入家庭组、成员邀请、守护关系和家庭高风险通知机制，使风险识别结果可以跨越单一用户边界，在高风险事件出现时同步给守护人。这一设计将反诈从“个人自救”升级为“家庭联防”，增强了系统在真实社会场景中的保护能力。

如图 13 所示，项目通过家庭组、守护关系和通知链路把个人风险识别扩展为家庭协同防护。

### 5.5 从识别工具到可运营平台的创新

> 图位占位建议：`图 14 知识治理与平台运营图`
>
> 图注建议：展示案例送审、管理员审核、后台采集、正式入库、图谱分析和后续召回的治理链路，体现平台化能力。

本项目不仅面向用户提供分析服务，也在管理员侧构建了待审核案例管理、后台案件采集、案件统计总览和知识图谱画像能力。其价值在于，系统不只是前台识别工具，而是具备知识治理、内容运营和持续优化能力的平台型系统。通过“案例送审 - 管理审核 - 正式入库 - 图谱分析 - 后续召回”的治理链路，平台能够持续吸收新型诈骗样本并反哺识别效果，这使项目在应用深度上明显区别于普通演示型作品。

同时，项目在进化机制上采取了审慎路线：我们认为反诈知识库直接服务于风险判定，若采用完全自动化入库，容易把错误样本直接扩散到后续识别环节。因此系统采用“自动化采集与清洗 + 人工审核把关”的半自动进化方案，在保持知识更新速度的同时控制错误传播风险。

如图 14 所示，系统通过案例治理、知识扩展和图谱运营能力，具备了平台级持续进化基础。

## 六、指标完成情况与应用效果

### 6.1 赛题指标对应完成情况

结合赛题要求与项目当前实现，本项目核心指标均已完成，具体对应关系如下：

| 赛题要求 | 对应实现 |
| --- | --- |
| 智能计算方向 | 已完成，多智能体、多模态分析、向量检索和知识增强构成智能计算核心 |
| 应用类项目 | 已完成，系统已落地为面向大众的 Android 产品与后端服务 |
| 多模态信息处理 | 已完成，支持文本、图像、音频、视频四类输入 |
| 智能识别与分析 | 已完成，支持风险等级判定、原因生成、结构化综合报告 |
| 用户服务与交互 | 已完成，具备移动端分析、聊天咨询、趋势查看和历史查看能力 |
| 主动预警 | 已完成，具备 WebSocket 主动提醒和后台通知能力 |
| 数据沉淀与持续优化 | 已完成，具备历史归档、用户记忆、案例送审、后台采集和知识图谱能力 |

如果进一步从赛题的“智能计算 + 应用类”两条主线来看，本项目不仅完成了基础功能实现，还在系统层面体现了智能计算的综合应用价值。一方面，多智能体分析、向量检索、知识增强和动态风险评估共同构成了项目的智能计算内核；另一方面，Android 移动端、实时推送、家庭联防和管理员治理能力则确保了项目具备明确的应用落地形态。这使项目在“技术可行”与“场景可用”两方面都具备较强说服力。

### 6.2 测评集评估结果

为验证系统在真实诈骗场景下的识别性能，项目团队自行构建了标准化测试数据集，并在该数据集上完成了系统评估。

#### 6.2.1 测评集构成

测评集共包含 **【待填写，不少于20】** 个案例，黑白样本比例为 1:1，黑样本覆盖以下诈骗类型：

| 序号 | 诈骗类型 | 样本数量 | 模态类型 |
| --- | --- | --- | --- |
| 1 | 投资理财诈骗 | 【待填写】 | 文本 |
| 2 | 兼职刷单诈骗 | 【待填写】 | 文本 |
| 3 | 冒充客服退款诈骗 | 【待填写】 | 文本 / 图片 |
| 4 | 虚假贷款诈骗 | 【待填写】 | 文本 |
| 5 | AI 合成语音诈骗 | 【待填写】 | 音频 |
| 6 | 冒充公检法诈骗 | 【待填写】 | 文本 / 音频 |
| 7 | 虚假征信诈骗 | 【待填写】 | 文本 |
| 8 | 游戏交易诈骗 | 【待填写】 | 文本 / 图片 |
| 9 | 追星诱导诈骗 | 【待填写】 | 图片 |
| 10 | 视频深度伪造诈骗 | 【待填写】 | 视频 |
| — | 正常样本（白样本） | 【待填写】 | 文本 / 图片 / 音频 |

#### 6.2.2 评估指标

在测评集上运行系统后，各项指标如下：

| 指标 | 结果 | 赛题要求 |
| --- | --- | --- |
| 准确率（Accuracy） | 【待填写】% | > 90% |
| 精确率（Precision） | 【待填写】% | — |
| 召回率（Recall） | 【待填写】% | — |
| F1-score | 【待填写】 | — |
| 误报率（正常样本误判为诈骗） | 【待填写】% | < 5% |
| 文本/图片平均响应时延 | 【待填写】秒 | < 10 秒 |
| 音频平均响应时延 | 【待填写】秒 | — |
| 视频平均响应时延 | 【待填写】秒 | — |

#### 6.2.3 评估说明

测评采用批量运行方式，将测评集案例逐条提交至系统多模态分析接口，记录系统输出的风险等级（高/中/低），以高风险和中风险判定为正例，低风险判定为负例，与标注标签对比后计算上述指标。响应时延取从请求发出到系统返回结构化报告的端到端时间均值。

### 6.3 核心能力完成情况

目前系统已经完成以下关键能力建设：

1. 多模态任务分析主链路。
2. 单图快速风险识别能力。
3. 主智能体工具闭环分析。
4. 防诈知识库语义检索。
5. 用户历史案件个性化记忆。
6. 历史案件归档与风险趋势总览。
7. 实时风险预警。
8. 家庭守护与联动通知。
9. Android 聊天式反诈助手。
10. Android 悬浮球快捷识别。
11. Android 无障碍自动守护。
12. 管理员案件审核、后台采集和图谱统计能力。

在此基础上，项目还进一步实现了诈骗链路还原、风险关键词提取、多轮对话反诈引导、用户风险趋势可视化和管理员知识图谱画像等增强功能。这些能力虽然不一定是赛题最基础的硬性要求，但能够显著提升系统完整性、展示表现力和评委对项目深度的感知。

### 6.4 系统应用效果

从业务效果看，本项目已经具备完整的系统运行闭环。用户可以在移动端主动提交可疑内容，系统通过多智能体联合分析后输出结构化结果；分析结果将同步沉淀到历史记录，形成用户个人风险记忆；当出现中高风险案件时，系统可主动推送风险提醒；当高风险用户建立家庭守护关系后，系统还可触发家庭成员联动通知；当识别到具有代表性的诈骗案例时，系统可继续将其送入待审核案例库，反哺知识库持续扩展。

这说明项目不只是“展示一次 AI 分析结果”，而是真正形成了从输入、分析、反馈、沉淀、联动到进化的完整产品闭环。

从用户视角看，系统能够在“发现可疑内容时快速分析”“长期查看个人风险变化”“在高风险时收到及时提醒”“在关键时刻让家人获知风险”四个层面提供直接价值；从平台视角看，系统能够通过案例沉淀、待审核治理、后台采集和统计总览不断扩展和优化自身能力。因此，项目同时满足了用户端实用性与平台端持续运营性的双重要求。

### 6.5 竞争力分析

如果从比赛视角看，本项目相较于一般参赛作品具有以下显著优势：

1. 题目契合度高，聚焦大众反诈这一强社会价值场景。
2. 项目完成度高，不是接口堆叠，而是完整产品系统。
3. 创新点丰富且成体系，不是零散功能堆砌。
4. 具备移动端真实交互与主动守护能力，演示效果强。
5. 具备知识沉淀与平台治理能力，体现长期价值。

## 七、可行性分析及风险管理

### 7.1 技术可行性

项目采用的核心技术均具备明确的实现基础。后端使用 Go、Gin、GORM、SQLite、Redis 等成熟技术构建稳定服务；模型能力通过 OpenAI 兼容协议接入，具备灵活配置能力；向量检索、WebSocket 推送、SSE 流式通信和多智能体工具链均已在项目中落地。移动端使用 Android Compose、前台服务、无障碍服务、截图能力等实现主动守护入口，技术方案完整可行。

### 7.2 经济可行性

本项目主要基于通用软件框架和现有模型能力构建，不依赖昂贵硬件和复杂专有部署环境。通过模块化设计和现有能力复用，系统具备较高的开发效率和较好的后续扩展空间，适合作为比赛项目和后续产品原型继续推进。

### 7.3 法律与合规可行性

项目强调用户授权、风险辅助和守护边界，所有主动守护能力均基于用户显式授权开启。系统用于风险提示和辅助判断，不直接替代法律认定或执法行为，具备较强的合规意识和应用边界控制能力。

### 7.4 市场可行性

当前面向普通公众、兼具多模态分析、主动预警和家庭联防能力的反诈产品仍较少见。本项目从产品场景、技术能力和交互形态上都更贴近大众真实需求，具备一定的推广价值和延展潜力。

### 7.5 风险及防范策略

项目后续仍需关注以下风险：

1. 诈骗手法持续进化，需要持续更新知识库和案例样本。
2. 多模态模型推理存在结果波动，需要通过知识增强与结构化工具闭环降低风险。
3. 主动守护能力涉及多项移动端权限，需要在产品说明和使用引导中强化授权边界。
4. 随着任务量增长，任务队列与调度能力仍需进一步升级。

针对上述风险，项目已通过案例审核机制、后台采集、知识治理、模块解耦和任务状态统一管理等方式建立初步防范基础。

## 八、团队构成与项目推进思路

### 8.1 项目推进逻辑

本项目遵循“问题驱动 - 架构搭建 - 能力闭环 - 产品打磨 - 材料组织”的推进思路。项目早期首先聚焦大众反诈的真实痛点，随后构建多模态分析主链路和知识沉淀能力，再逐步扩展聊天系统、个性化记忆、实时告警、家庭联防和移动端主动守护能力，最终形成完整的系统级作品。

### 8.2 模块化协同思路

项目设计坚持低耦合原则，不将新功能简单塞入原有模块，而是通过登录系统、聊天系统、用户画像系统、家庭系统、多智能体分析系统、知识库模块和缓存模块进行分层组织。各模块边界清晰，既保证了工程可维护性，也有利于参赛文档和答辩中的系统性表达。

### 8.3 项目材料优势

从参赛材料角度看，本项目具备以下优势：

1. 有明确的选题价值与社会意义。
2. 有清晰的技术方案与产品闭环。
3. 有真实可演示的 Android 软件端。
4. 有创新点总结、架构说明、接口文档和数据库说明等完整支撑材料。
5. 有较强的答辩可塑性，能够围绕“面向大众的反诈系统”进行统一叙事。

## 九、项目总结与展望

本项目围绕“面向大众的多模态反诈智能系统”这一目标，完成了从多模态风险识别到移动端产品落地的系统性建设。项目不仅支持对文本、图片、音频、视频等多类诈骗信息进行联合分析，还进一步实现了用户历史记忆、风险趋势总览、实时预警、家庭联防、知识审核入库、后台案件采集和知识图谱画像等能力，形成了较强的系统闭环。

与一般仅提供单次判断结果的反诈工具相比，本项目更强调“长期守护”这一核心价值。系统既帮助用户识别当前风险，也努力帮助用户和家庭形成持续防护能力。这种从“工具型识别”走向“系统型守护”的升级，是本项目最具竞争力的核心所在。

未来，项目仍可在以下方向进一步深化：

1. 升级任务调度能力，支持更稳定的大规模任务处理。
2. 持续丰富历史案例知识库，提升新型诈骗发现能力。
3. 优化移动端交互体验，增强产品完成度与用户易用性。
4. 深化社区、校园、家庭等典型场景应用，提升推广可行性。

总体而言，本项目在赛题契合度、系统完整性、技术创新性、产品落地性和展示表达力等方面均具备较强优势，是一套具有明显获奖竞争力的面向大众反诈系统方案。
