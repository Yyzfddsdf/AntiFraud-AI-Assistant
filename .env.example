# Server
PORT=8081

# Auth/JWT
JWT_SECRET=replace-with-your-jwt-secret
INVITE_CODE_ADMIN=replace-with-admin-invite-code

# SQLite database paths
DB_PATH=DB/auth_system.db
HISTORICAL_CASE_DB_PATH=DB/historical_case_library.db

# Redis (chat context) - optional if your runtime provides defaults elsewhere
# REDIS_ADDR=127.0.0.1:6379
# REDIS_PASSWORD=
# REDIS_DB=0

