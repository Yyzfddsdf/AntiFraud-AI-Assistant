# 前端目录

当前前端拆成两个独立服务：

- `desktop-vue/`：桌面端独立 Vue 工程
- `mobile-vue/`：移动端独立 Vue 工程

## 启动

桌面端：

```bash
cd frontend/desktop-vue
npm install
npm run dev
```

默认地址：`http://localhost:5173`

移动端：

```bash
cd frontend/mobile-vue
npm install
npm run dev
```

默认地址：`http://localhost:5174`
